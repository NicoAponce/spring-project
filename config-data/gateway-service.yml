server:
    port: 8080
  
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
  
spring:
    cloud:
        gateway:
            discovery:
                locator:
                    enabled: true
            routes:
            -   id: customer-service
                uri: file:/tmp/config-repo-3216944306718599490/config-data/
                predicates:
                - Path=/customers/**