server:
  port: 8002
spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:file:~/test
    username: sa
    password:
    driverClassName: org.h2.Driver
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8004/eureka/
