server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/NicoAponce/spring-project.git
          search-paths: config-data
          username: ${GIT_USER}
          passphrase: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

